/**
 * Timer class
 */
"use strict";

/**
 * Constructor for the Timer Class
 * @param {[type]} name         [description]
 * @param {[type]} timerLength  Maximimum countdown length of the timer in seconds (default 60 seconds)
 * @param {[type]} timerCounter [description]
 */
function Timer(name, timerLength, timerCounter) {
  name = name;
  length = typeof length !== 'undefined' ? counter : 60;
  counter = typeof timerCounter !== 'undefined' ? counter : 1;
  remaining = timerLength;
}

Timer.prototype = {
  constructor: Timer,
  startTimer:function() {

  },
  pauseTimer:function() {

  },
  resumeTimer:function() {

  }
  resetTimer:function() {

  }
}
